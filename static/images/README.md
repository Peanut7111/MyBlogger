# 背景图片说明

## 当前背景

`zelda-bg.jpg` — 塞尔达主题背景图，建议压缩到 500KB 以下。

## 如何替换

1. 将新图片命名为 `zelda-bg.jpg` 或 `zelda-bg.png`
2. 放入此文件夹（`static/images/`）
3. 如需修改路径，编辑 `static/css/zelda.css` 中的 `background-image`

## 推荐图片尺寸

- 宽度：1920px 或更大
- 高度：1080px 或更大
- 格式：JPG（压缩率高）
- 大小：< 500KB（建议）
